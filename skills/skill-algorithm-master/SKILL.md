---
name: skill-algorithm-master
description: |
  算法全能工具箱 — 查询即用、研究即深入、实现即生产。

  **LOOKUP 触发（满足任一）：**
  - 用户问"用什么算法"、"有没有现成的"、"XXX 复杂度"
  - 用户要求"实现 LRU"、"写个堆"、"并查集怎么写"
  - 用户问数据结构（堆、线段树、布隆过滤器、Trie）
  - 用户说"太慢了"、"O(n²)"、"优化这段代码"

  **RESEARCH 触发（满足任一）：**
  - 用户说"研究这个算法"、"帮我理解 XXX 算法原理"
  - 用户说"分析下这个算法"、"它和 YYY 有什么区别"
  - 用户提到具体算法名字（Leiden、PageRank、A*、CRDT）

  **不触发：**
  - 用户在描述业务逻辑，无性能/算法问题 → 不触发
  - 用户要求完整系统设计 → 用 system-design skill
  - 用户要的是数据库/操作系统/网络协议 → 用对应 skill

  **两种风格**：竞赛版（最短最快）vs 工程版（健壮可维护）
  **支持语言**：Python / JavaScript / TypeScript / Rust / Go / C++
---

# skill-algorithm-master

算法全能工具箱。Lookup 模式秒级响应，Research 模式深度剖析。

## 决策树

```
用户问题
  │
  ├─ 知道算法名，要查代码/复杂度
  │    → 走 [LOOKUP 模式]
  │    → 快速决策表 → references/algorithms.md 提取代码
  │
  ├─ 不知道用什么，要分析问题再推荐
  │    → 走 [ANALYZE 模式]
  │    → 识别问题类型 → 匹配算法 → 给出代码
  │
  ├─ 要研究/理解/对比一个具体算法
  │    → 走 [RESEARCH 模式]
  │    → 读 references/research-sop.md → 执行 7 步研究
  │
  └─ 要对比两个算法的优劣
       → RESEARCH 模式 + 对比表
```

---

## LOOKUP 模式：快速决策表

根据问题关键词直接匹配。

| 问题关键词 | 推荐算法 | 复杂度 | 风格 |
|-----------|---------|--------|------|
| 第 K 大/小 | 快速选择 | O(n) | 竞赛 |
| 频繁查找 | 哈希表 | O(1) | 工程 |
| 范围查询 | 线段树 | O(log n) | 工程 |
| 前缀和动态更新 | 树状数组 BIT | O(log n) | 工程 |
| 去重/存在性判断 | 布隆过滤器 | O(k) 空间 | 工程 |
| 字符串精确匹配 | KMP | O(n+m) | 竞赛 |
| 字符串多模式 | AC 自动机 | O(n+m) | 工程 |
| 最短路径 | Dijkstra | O(E log V) | 工程 |
| 最小生成树 | Kruskal / Prim | O(E log E) | 工程 |
| 连通性判断 | 并查集 Union-Find | O(α(n)) | 竞赛 |
| 排行榜 Top-K | 堆 / 快速选择 | O(n log k) | 视场景 |
| LRU 缓存 | 双向链表+哈希 | O(1) | 工程 |
| 图社区检测 | Leiden / Louvain | O(n log n) | 工程 |
| 任务调度/贪心 | 堆 + 排序 | O(n log n) | 竞赛 |

**选风格**：生产环境默认工程版，追求极限性能用竞赛版。

**匹配流程**：先判断问题属于哪个算法类，再看复杂度要求，最后选风格出代码。

---

## ANALYZE 模式：问题→算法推荐

当用户没有给出具体算法名时，分析问题类型再推荐。

### 问题类型识别

| 问题模式 | 背后算法 | 推荐理由 |
|---------|---------|---------|
| "从 N 个里找 Top-K" | 堆 or 快速选择 | 堆适合实时，QuickSelect 适合一次性 |
| "有重复要去重" | 哈希表 or 布隆过滤器 | 数据小用哈希，数据极大用布隆 |
| "要快速判断存不存在" | 布隆过滤器 or 哈希 | 布隆省空间，哈希精确 |
| "要算区间和" | BIT or 线段树 | 简单前缀和用 BIT，复杂合并用线段树 |
| "最短路径/最优解" | Dijkstra / A* / DP | 权值非负用 Dijkstra，有启发用 A* |
| "迭代更新、慢慢收敛" | 贪心 or 模拟退火 | 看是否有最优子结构 |
| "分层/聚类" | 社区检测 / 层次聚类 | Leiden / Louvain for graph |

### 推荐输出格式

```
## 分析结论

**问题类型**：[分类]
**推荐算法**：[名字]
**复杂度**：[时间] / [空间]
**不适合场景**：[如果有]
**选择原因**：[一句话]
```

---

## RESEARCH 模式：深度研究流程

**当用户要研究/理解/对比一个具体算法时**，读 `references/research-sop.md` 获取完整 7 步研究流程，并按其指引执行。

```
触发 RESEARCH 模式
  ↓
读 references/research-sop.md（完整 SOP + 模板）
  ↓
S1 定性 → S2 对比 → S3 原理 → S4 源码 → S5 泛化 → S6 映射 → S7 归档
  ↓
研究成果 → 追加到 references/algorithms.md
       → 可选写 memory/research/algorithms/[算法名]/
```

---

## 快速参考

### 数据结构 5 问

| 问题 | 答案 |
|------|------|
| 查「是否存在」→ 精确 | 哈希表 |
| 查「是否存在」→ 可能误判 | 布隆过滤器 |
| 查「Top-K」→ 实时 | 堆 |
| 查「Top-K」→ 一次性 | 快速选择 |
| 查「区间和」→ 简单 | BIT |
| 查「区间和」→ 复杂合并 | 线段树 |
| 合并「集合」+ 查「连通」 | 并查集 |

### 复杂度直观对照（n = 10⁶）

| 复杂度 | 实际感受 |
|--------|---------|
| O(1) | 瞬间 |
| O(log n) | ~20 步 |
| O(n) | 10⁶ 步，可接受 |
| O(n log n) | ~2×10⁷ 步，边缘 |
| O(n²) | 10¹² 步，不可行 |

### references/algorithms.md 已有算法

- 并查集 / 线段树 / 树状数组 / 堆 / LRU
- 快速选择 / KMP / Dijkstra / Kruskal
- Leiden / Louvain 社区检测

---

## 输出规范

**Lookup 响应**：算法名 + 一句话定性 + 时间/空间复杂度 + 代码（竞赛版 + 工程版）+ 适用条件 + 不适用条件

**Research 响应**：按 `references/research-sop.md` 的完整报告格式，7 个 section 全部覆盖

**Analyze 响应**：问题分类 + 推荐算法 + 推荐理由 + 一句话定性

---

## 参考文件

| 文件 | 内容 | 何时读 |
|------|------|--------|
| `references/algorithms.md` | 全部算法代码库 | LOOKUP 模式提取代码时 |
| `references/research-sop.md` | 完整 7 步研究 SOP | RESEARCH 模式执行研究时 |
| `references/evaluation.md` | 量化评价体系 + P0-P3 错误分级 | 研究完成后自检时 |
| `memory/research/algorithm-sop.md` | 同 SOP 存档 | 快速参考 |

---

## 新增算法到本工具箱

当研究完一个新算法并通过评价体系自检后：

1. 追加到 `references/algorithms.md`（格式模板在 research-sop.md S7 节）
2. 在 SKILL.md 快速决策表追加一行
3. 在 `memory/research/algorithms/[算法名]/` 创建完整研究文档（参考 SOP S7 节）
