# fmt skill

格式化代码文件，支持所有主流语言。

## 使用方式

```
@feather fmt <文件路径>
```

或直接指定多个文件：
```
@feather fmt file1.js file2.ts
```

## 支持的文件类型

`.js` `.ts` `.jsx` `.tsx` `.json` `.md` `.css` `.scss` `.html` `.yaml` `.yml` `.vue` `.svelte`

## 示例

```
@feather fmt src/index.js
@feather fmt src/**/*.ts
```
